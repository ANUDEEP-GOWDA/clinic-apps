// MobileNav lives inside Header.tsx as a controlled drawer to avoid a
// separate client component just for state. This file is intentionally a
// re-export shim to honor the folder structure listed in the spec.
export { default } from './Header';
